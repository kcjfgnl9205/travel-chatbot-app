[Unit]
Description=travel-chatbot-app 자동 배포 타이머 (1분마다)

[Timer]
OnBootSec=2min
OnUnitActiveSec=1min
# 이전 실행이 아직 안 끝났으면 건너뛴다 (remote.sh 의 flock 과 이중 안전장치)
AccuracySec=10s
Unit=travel-chatbot-deploy.service

[Install]
WantedBy=timers.target
